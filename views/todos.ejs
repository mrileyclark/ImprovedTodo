<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Todos</title>
    <link
    rel="stylesheet"
    href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css"
  />
  <link rel="stylesheet" href="css/style.css">
  <script
  src="https://kit.fontawesome.com/d6ab8d0597.js"
  crossorigin="anonymous"
></script>
</head>
<body>
<section id="container">
    <h1 id="heading">Todos</h1>
    <ul>
    <% todos.forEach( el => { %>
            <li class='todoItem' data-id='<%=el._id%>'>
                <span class='<%= el.completed === true ? 'completed' : 'not'%>'><%= el.todo %></span>
                <span class='del'><i class="fa-solid fa-trash-can"></i></span>
            </li>
    <% }) %>    
    </ul>

    <h2 id="heading2"><%= user.userName %> has <%= left %> things left to do</h2>

    <form action="/todos/createTodo" method='POST' class="signup-form">
        <input type="text" placeholder="Enter Todo Item" name='todoItem' class="todo-form">
        <input type="submit" class="btn todo-form"/>
    </form>
    <a href="/logout" class="links">Logout</a>
</section>
   

    <script src="js/main.js"></script>
</body>
</html>